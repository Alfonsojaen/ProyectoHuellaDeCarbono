package github.alfonsojaen.test;

import github.alfonsojaen.dao.UserDAO;
import github.alfonsojaen.entities.Huella;
import github.alfonsojaen.entities.Usuario;
import github.alfonsojaen.services.HuellaService;
import github.alfonsojaen.singleton.UserSession;

public class GuardarHuellaTest {
    public static void main(String[] args) {/*
        // 1. Iniciar sesión con un usuario
        Usuario usuario = new Usuario();
        usuario.setEmail("alvaro@gmail.com");
        usuario.setContraseña("alvaro");
        UserSession.getInstancia().logIn(usuario);        // Registrar el usuario en el singleton de sesión

        // 2. Crear una instancia de UserDAO
        UserDAO userDAO = new UserDAO();

        // 3. Crear un objeto Huella con datos de prueba
        Huella nuevaHuella = new Huella();
        nuevaHuella.setId(1);
        nuevaHuella.setValor(3.5);
        nuevaHuella.setUnidad("mg/dL");
        nuevaHuella.setFecha("2023-06-01");
        nuevaHuella.setIdActividad(1);

        // 4. Comprobar el método crearHuella
        boolean resultado = crearHuella(nuevaHuella, userDAO);

        // 5. Mostrar el resultado de la operación
        if (resultado) {
            System.out.println("La huella se creó correctamente.");
        } else {
            System.out.println("No se pudo crear la huella.");
        }*/
    }
}
